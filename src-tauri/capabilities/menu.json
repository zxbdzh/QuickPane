{
  "$schema": "./gen/schemas/desktop-schema.json",
  "identifier": "menu-popup",
  "description": "Capability for the menu popup window (self-hide)",
  "windows": ["menu"],
  "permissions": [
    "core:default",
    "opener:default",
    "core:window:allow-hide"
  ]
}
